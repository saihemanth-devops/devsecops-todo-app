apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: todo-vs
spec:
  hosts:
  - "*"
  gateways:
  - todo-gateway
  http:
  - route:
    - destination:
        host: todo-app-service
        subset: v1
